name: SmartSwing CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run PowerShell smoke suite
        shell: pwsh
        run: |
          ./tests/run-tests.ps1
